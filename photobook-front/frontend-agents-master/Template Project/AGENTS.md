# Archived Template Rules

This folder is a historical pre-Photobook snapshot. Do not use its copied
`agent-assets/` as active project instructions and do not copy this template
into a new project.

Use the canonical assets in `../agent-assets/` and the current installer, or
follow the consuming Photobook project's root `AGENTS.md`.
