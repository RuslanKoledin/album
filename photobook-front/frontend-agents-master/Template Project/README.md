# Archived Template Project

This directory preserves the original generic template for reference. It
predates the Photobook adaptation and contains conflicting TanStack, Zustand,
Next.js, shadcn, documentation, and offline-blocker defaults.

Do not copy or activate it. Use `../agent-assets/` and
`../tools/install-agent-assets.ps1` as the canonical source.
