# Archived Template Snapshot

## Status

Archived. Do not use for project bootstrap.

The directory preserves the original generic bundle so its provenance is not
lost. Canonical maintained assets live in `../agent-assets/`, and the current
installer generates the project-local `AGENTS.md` block.
