---
type: workflow
status: deprecated
updated: 2026-07-21
sources:
  - Template Project/AGENTS.md
  - Template Project/README.md
tags:
  - project-docs
  - archive
  - template
---

# Archived Template Project

`Template Project/` preserves the generic pre-Photobook bundle. It contains
stale stack and governance defaults and must not be copied or activated.

Use canonical `agent-assets/` and `tools/install-agent-assets.ps1`, or connect
the canonical skills through the consuming project's root `AGENTS.md`.
