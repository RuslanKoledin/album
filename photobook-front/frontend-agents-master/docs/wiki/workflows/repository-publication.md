---
type: workflow
status: current
updated: 2026-05-29
sources:
  - ../../.git/config
tags:
  - project-docs
  - wiki/workflow
---

# Repository Publication

## GitHub

- Repository: https://github.com/AzamatRaimbekov/frontend-agents
- Slug: `frontend-agents`
- Visibility: private
- Local remote: `origin`
- Published branch: `master`
- First commit: `c5f90d7 Initial commit`

## GitLab

- Status: blocked until GitLab credentials are available locally.
- Checked sources: `glab` command, `GITLAB`/`GL_` environment variables, Git Credential Manager, `.config` GitLab folders, and `.netrc`.
- Next action: authenticate GitLab or provide a GitLab personal access token with permission to create projects, then create `frontend-agents` and add a `gitlab` remote.
